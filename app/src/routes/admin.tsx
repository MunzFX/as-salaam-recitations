import {createFileRoute,Outlet} from '@tanstack/react-router';export const Route=createFileRoute('/admin')({head:()=>({meta:[{title:'Administrator | As-Salaam Institute'},{name:'robots',content:'noindex,nofollow'}]}),component:Outlet});