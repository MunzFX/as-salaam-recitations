import {createFileRoute} from '@tanstack/react-router';export const Route=createFileRoute('/robots.txt')({server:{handlers:{GET:({request})=>new Response('User-agent: *\nAllow: /\nDisallow: /admin\nDisallow: /api/\nSitemap: '+new URL(request.url).origin+'/sitemap.xml',{headers:{'Content-Type':'text/plain'}})}}});